# Offline Whisper model

Whisper model weights are deliberately excluded from desktop installers and update ZIPs.

Use the in-app **Download Offline** button. For local development, `scripts/download-whisper-model.ps1` installs the model into writable private storage.
